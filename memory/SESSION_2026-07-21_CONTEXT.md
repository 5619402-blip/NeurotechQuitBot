# NeuroTech Quit — Контекст сессии 21.07.2026 (Cowork / Claude)

**Назначение файла:** прочитать в начале любого нового чата, чтобы восстановить полный контекст работы.
Владелец: Альберт. Правило работы: **пошагово — одна правка за раз**, Claude ничего не делает без явного «давай».

---

## 1. Что было сделано в этой сессии

1. Прочитан весь код бота (callbacks.js целиком, все 42 экрана, роутер, db-слой, cron, плеер, ТЗ v4).
2. Проведён полный аудит кода против ТЗ v4 → HTML-отчёт (артефакт «neurotech-quit-audit» в галерее Cowork).
3. Составлен список доработок (см. раздел 3).
4. Созданы универсальные скрипты: `scripts/reset_user.js <id> --confirm [--keep-user]` и `scripts/fast_forward.js <id> --confirm` (лежат в папке проекта, на сервер ещё НЕ задеплоены).
5. Изучено боевое окружение (см. раздел 4).
6. Сброшены все тестовые аккаунты, сделан бэкап БД: `/app/data/backup-2026-07-21.sqlite`.
7. Начат живой QA-проход владельцем (см. memory/QA_WALKTHROUGH_2026-07-21.md).

## 2. Боевая инфраструктура (проверено глазами)

- **Хостинг бота:** Bothost (bothost.ru), бот `bot_1779722921_7010_marko`, «Neurotech Quit», Node.js, статус running.
  ⚠️ **Подписка Bothost истекала через 4 дня от 21.07 — продлить!**
- **Деплой:** GitHub `5619402-blip/NeurotechQuitBot` → кнопка «Обновить из Git» в панели Bothost (есть и webhook автообновления — не настроен). Деплой занимает время — ждать, проверять логи.
- **БД на проде: SQLite** (`/app/data/neurotech_quit.sqlite`), DATABASE_URL НЕТ. DATA_DIR=/app/data. CRON_ENABLED=true.
- **Плеер:** Vercel, `https://neurotech-quit-bot.vercel.app`. Mux: все 5 playback ID заданы (вкл. short).
- **ENV на проде:** нет ADMIN_TELEGRAM_IDS (админ только вшитый 7185030567), нет CLOUDFLARE_TUNNEL_TOKEN (туннель quick-режим, случайный URL при рестарте).
- **Терминал Bothost:** popup `terminal.html?bot=...`; заявлен белый список команд, но `cp` и `node` фактически работают. Команды в терминал вставляет владелец (Claude туда доступа не имеет — блок классификатора).
- **Сайт:** neurotechquit.com, статика на Cloudflare, исходники `~/Projects/neurotechquit.com` (deploy/index.html = site-v1/index.html). CTA ведут в бот с метками `?start=hero|steps|access|stories|final` — бот их игнорирует.
- **Другой проект владельца:** PARADOX (`~/Projects/ClaudeAgentSkillsLab/projects/paradox`) на Beget Cloud VPS 217.26.28.127 (2 CPU/4ГБ, Docker, paradox-app.com). **План: перенести бота туда** (поддомен + HTTPS + PostgreSQL) — непосредственно перед подключением кассы.
- Тестовые аккаунты: 312561696 (Альберт #2), 7185030567 (Альберт, ГЛАВНЫЙ АДМИН — сбрасывать только щадяще reset_admin!), 967110140 (Юля).

## 3. Список доработок (статус на конец сессии)

Критичные: 1) callback плеера + убрать кнопки «(тест)» ← СЛЕДУЮЩАЯ ПРАВКА, ждёт «давай»; 2) таймаут abandoned; 3) касса (+PAYMENT_TEST_MODE не проверяется).
Баги: 4) Q3–Q6 недостижимы; 5) next-day опрос не создаётся; 6) Альфа затирает user_status; 7) даты/PG-синтаксис в reminders (на SQLite повторные напоминания не работают — подтверждено, чинить под SQLite); 8) ссылка плеера 5 мин без пересоздания; 9) тупик «Процедура уже активна» без кнопок; 10) legacy /player с паузой.
Решения владельца: 11) gift-текст обещает Альфу, конфиг даёт 0; 12) Альфа в тарифе 990; 13) мотивация 1–3 жёсткий стоп vs «всё равно продолжить»; 14) вопросы возраст/тип никотина; 15) отзывы в Telegram file_id; 16) вопросы после Альфы.
Чистка: 17) ✅ ЗАКРЫТ — Welcome без кнопки диагностики принят владельцем; 18) TEMP-логи; 19) support screen хардкод; 20) хардкод админ-ID/username; 21) QA-скрипты в scripts/; 22) статусы-призраки; 23) events + фиксация ?start=меток с сайта; 24) процесс ответа поддержки.
Новые: 25) кнопка «Открыть/Запустить» у бота как в PARADOX (Mini App menu button — потом); 26) setMyCommands, чтобы /start был кнопкой в меню; 27) в витринных отзывах показываются тех. имена файлов (VIDEO-2023-...mp4) вместо имени клиента — поправить данные в таблице reviews.
Перед кассой: оферта и политика на сайт (/offer, /privacy) — обязательное требование провайдера.

## 4. Процесс работы (согласовано с владельцем)

1. Одна правка за раз: объяснение → «давай» → правка кода → деплой → владелец проверяет в Telegram → «ок» → следующая.
2. Claude правит код в папке на Mac и вносит те же правки на GitHub через браузер владельца (Chrome + панель Bothost доступны Claude; терминал и env-viewer — нет).
3. Скрипты в терминале Bothost запускает владелец (Claude готовит команды).
4. Сбросы: полный `reset_user_<id>.js --confirm-reset-<id>`; админа — ТОЛЬКО `reset_admin_7185030567.js`.
